const {local_storages, http_headers} = require('../config');

// pre page execute
module.exports = async (page) => {
    // init params, data before performing test actions
    // you can retreave local storage and/or http headers data to use it before performing any actions
};